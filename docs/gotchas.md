# Gotchas — canonical list

A discovered pitfall is documented here so the next session does not
re-discover it the hard way. Add new rows as they emerge, with the
date and the commit / context where the lesson was learned.

| # | Symptom | Root cause | Fix | First seen |
|---|---|---|---|---|
| 1 | Phase 2 v1 build failed: KCM branding test — "expected Variant=…, got ''" | `/usr/share/kcm-about-distro/kcm-about-distrorc` doesn't exist on Bazzite (Aurora-only path); a `[ -f ]` guard on it makes the branding step silently no-op. | Bazzite ships the file at `/etc/xdg/kcm-about-distrorc` (the Bazzite-DX-style location). `build_files/mx/00-image-info.sh` seds `Variant` and `Website` there + updates `image-info.json` (image-name + image-ref + image-vendor) and `/usr/lib/os-release` VARIANT_ID. Smoke test asserts all six values (image-name, image-vendor, image-ref, VARIANT_ID, Variant, Website) to prevent silent regression. | Phase 2 (branding) |
| 2 | `dnf5 config-manager setopt <id>.enabled=0` returns 0 but the .repo file is unchanged | dnf5 5.x: setopt is a silent no-op on .repo files added via `addrepo --from-repofile=URL` or `--repofrompath` | `sed -i 's/^enabled=1/enabled=0/g' /etc/yum.repos.d/<file>.repo` — same idiomatic pattern used everywhere we touch a runtime-added .repo | Phase 3 (container runtime) |
| 3 | `systemctl enable swtpm-workaround.service` → "Unit … does not exist" | The COPR `ublue-os-libvirt-workarounds` v1.1+ consolidated the separate `swtpm-workaround.service` into the single `ublue-os-libvirt-workarounds.service` (a SELinux relabel oneshot) | Don't enable the missing unit. Modern swtpm + libvirt work without a separate workaround service. Aurora upstream's build script references the old name — stale code; don't copy it blindly | Phase 4 (virt) |
| 4 | `bootc container lint` warns `nonempty-boot: Found non-empty /boot: extlinux` (and `nonempty-run-tmp: Found content in runtime-only directories (/run, /tmp)`) | `qemu-system-x86-core` drops an `extlinux` binary in `/boot` for VM bootloader templates; the build stage leaves transient content in /run and /tmp | Non-blocking warnings; both are the build's known lint-residue signature — a third warning (or zero) means something changed. If extlinux ever needs to go, add a `find /boot -name extlinux -delete` to `clean-stage.sh`, but only with a clear rationale | Phase 4 (virt) + audit baseline 2026-07-06 |
| 5 | `ujust setup-virtualization virt-on` misbehaves on bazzite-mx without our override: the full-recipe variant silently no-ops, Bazzite's own recipe installs a duplicate flatpak | The full recipe (bazzite-dx's `84-bazzite-virt.just` override) is gated on `if ! rpm -q virt-manager \| grep -P "^virt-manager-"` — it **only** runs the kargs/swtpm/hooks setup if virt-manager is NOT RPM-installed. On bazzite-mx, `20-virtualization.sh` installs virt-manager as RPM at build time, so that gate is permanently FALSE and the entire setup branch is skipped. Bazzite's own recipe is flatpak-only (commit `8f1c46b4`): its `virt-on` installs the org.virt_manager.virt-manager flatpak whenever `flatpak info` misses it — on bazzite-mx that branch would always fire, duplicating the RPM and fighting the flatpak blocklist | (a) build-time enable libvirtd.service in `20-virtualization.sh` so the basic stack works on first boot without any recipe; (b) ship the KVM module options via `system_files/usr/lib/modprobe.d/bazzite-mx-kvm.conf` (see gotcha 18); (c) ship our `setup-virtualization` recipe in `system_files/usr/share/ublue-os/just/96-bazzite-mx-overrides.just` — the gate dropped and the flatpak-install line removed (we have RPM), while `build_files/mx/55-justfile-reconcile.sh` surgically removes the same-named upstream recipe from `84-bazzite-virt.just`. VFIO/kvmfr/usbhp blocks kept verbatim from bazzite-dx. Rationale and provenance: divergence #4 in [`upstream-divergences.md`](upstream-divergences.md) | Phase 4 (virt L2 hardening) |
| 6 | `dnf5 install iotop` succeeds, but `rpm -q iotop` returns "not installed" | F44 replaced the Python `iotop` with the C rewrite `iotop-c-1.31`; `iotop` is an alias provider that resolves to `iotop-c`. The binary at `/usr/bin/iotop` keeps the historic name; the package name does not. | Smoke test asserts `rpm -q iotop-c`; the install line in `40-dev-cli-rpms.sh` requests `iotop-c` directly | Phase 6 (CLI) |
| 7 | `curl -I https://release.gitkraken.com/...` returns HTTP 404 | Server rejects HEAD method | `curl -sL --range 0-1023 <url>` (GET partial) — also lets you `file` the bytes to verify it's an RPM | Phase 5 (IDE — fetched while validating GitKraken provenance) |
| 8 | `docker run` on a fresh deployment fails with "permission denied while trying to connect to the Docker daemon socket" even though `docker-ce` is installed and `docker.socket` is enabled | `docker-ce` from Docker Inc creates the `docker` group via an rpm postinstall scriptlet (`groupadd --system docker`). On rpm-ostree atomic systems, package postinstall scriptlets are SUPPRESSED to keep the OCI layer reproducible — so the group is never created. The `bazzite-mx-groups` hook then logs `WARNING: group docker not in /usr/lib/group; skipping` (visible in journal of first boot) and silently no-ops the docker membership grant. Aurora and Bazzite-DX inherit this gap (verified: neither ships a sysusers.d for docker). | Ship `/usr/lib/sysusers.d/bazzite-mx-docker.conf` with a single line `g docker -` (creates the group via systemd-sysusers at sysinit.target, before our hook runs). Bump `version-script bazzite-mx-groups system N` to invalidate the cached state on existing installations so the hook re-runs and adds wheel users to the now-existing docker group. Full story: divergence #12 in [`upstream-divergences.md`](upstream-divergences.md) | Phase 9 (bazzite-extras) |
| 9 | `ujust install-<x>` recipes shipped in `95-bazzite-mx.just` (or overrides in `96-bazzite-mx-overrides.just`) are not visible to ujust | Bazzite's master `/usr/share/ublue-os/justfile` uses explicit `import` directives (no glob). Files in `/usr/share/ublue-os/just/` not registered in the master are silently ignored. | `build_files/mx/55-justfile-reconcile.sh` idempotently registers both `95-bazzite-mx.just` **and** `96-bazzite-mx-overrides.just` in the master via `grep -qxF \|\| echo >> master`. Bazzite-DX and AmyOS do the same but without idempotency check. Full story: divergence #13 in [`upstream-divergences.md`](upstream-divergences.md) | Phase 10 (justfile) |
| 10 | Running `ujust install-1password` (or any `install-*` that calls a private helper) prints `error: Recipe '_pkg_layered' failed on line N with exit code 1` even when the install proceeds correctly | `just` always emits that line whenever a sub-recipe exits non-zero, even when the caller wraps the call in `if`. Our `_pkg_layered` helper used `jq -e` exit codes as a boolean signal — idiomatic in bash, but here it polluted the install-* output with a misleading "error" line. | Switch the helper to communicate via stdout: `@if jq -e ...; then echo yes; else echo no; fi`, always exits 0. Callers compare the string with `[ "$(just _pkg_layered <pkg>)" = "yes" ]`. Same DRY benefit, zero spurious lines. Full story: divergence #13 in [`upstream-divergences.md`](upstream-divergences.md) | Phase 10 (justfile) |
| 11 | `libsetup.sh::version-script` writes the versioned state file BEFORE the hook body runs. Under `set -euo pipefail`, a single failed command (transient marketplace timeout, missing skel file, …) aborts the hook AFTER the state has been committed → next login skips the hook → silent permanent disable | `libsetup.sh` design choice: state-first ensures atomicity and idempotency on success. The cost is that "failed run that already wrote state" is indistinguishable from "successful run" on next boot. | (a) Add `\|\| true` after each best-effort command (e.g., `code --install-extension X \|\| true`) — failure becomes benign, hook completes, state correctly reflects "I tried." (b) Guard ALL source paths used in the hook (`[ -e /etc/skel/... ]`). Full story: divergence #14 in [`upstream-divergences.md`](upstream-divergences.md) | Phase 11 (vscode-extensions hook code review) |
| 12 | DNF resolves `dnf5 install sunshine` (lowercase) successfully but `rpm -q sunshine` returns "package sunshine is not installed" | rpm-q is case-sensitive on the actual rpm name (`Sunshine` capitalized in the lizardbyte/stable COPR spec); DNF normalizes case at install time, masking the divergence. Smoke tests that use `rpm -q sunshine` would silently regress on every build. | Always use the **exact rpm-name capitalization** in `rpm -q` assertions. For Sunshine: `rpm -q Sunshine`. Aurora's `01-packages.sh` uses lowercase `sunshine` in the COPR install (works due to DNF case-insensitivity); we keep lowercase for symmetry but use capitalized `Sunshine` in tests. | Phase 12 (Sunshine reintegration) |
| 13 | Bazzite's `cockpit.service` `Requires=cockpit-container.service` but no such file exists in `/usr/lib/systemd/system/` | The unit is generated at boot from `/usr/share/containers/systemd/cockpit-container.container` (a podman quadlet) by `podman-systemd-generator` | Don't fight Bazzite's design — `ujust cockpit enable` works. Full quadlet rationale: [`architecture.md`](architecture.md) § Cockpit pattern | Discovery while scoping potential Cockpit phase (intentionally skipped) |
| 14 | `podman build … > log 2>&1; echo BUILD_EXIT=$?` always reports 0 even when the build failed | Final `echo` is the last command; its exit-zero replaces the build's exit code in the shell's overall exit status | Use `BUILD_EXIT=$?; echo "BUILD_EXIT=$BUILD_EXIT"; exit $BUILD_EXIT` (the explicit `exit` propagates the real status). Or use `&&` to chain failure-honouring | Pre-flight wrapper iteration |
| 15 | `paths-ignore` did not skip CI for a docs commit | Commit included `.claude/settings.json` and `.gitignore`, neither of which matches `**.md`, `LICENSE`, or `docs/**`. GitHub paths-ignore: workflow runs if **any** file fails to match | Either: (a) extend paths-ignore to cover `.claude/**` and `.gitignore` for future free docs commits, or (b) accept the redundant CI run and move on | Docs-only commit that triggered CI |
| 16 | AmyOS ships vendored `vscode.repo` with `enabled=1` (their philosophy), opposite to ours | Different distro philosophy — they trust the source and let it auto-update via rpm-ostree layering | We keep `enabled=0` and `validate-repos.sh` enforces it. Diverge consciously, document the decision (we do, in `vscode.repo`'s context comment) | Phase 5 (IDE) — comparing AmyOS approach |
| 17 | After `gh repo delete` + `gh repo create`, the first push triggers builds that fail at the `Sign container image` step | GitHub Secrets are repo-scoped and encrypted with the repo's public key; a recreated repo has a new internal ID + new pubkey, so old secrets are NOT migrated even if recreation is immediate | Reload **before** the first push: `gh secret set SIGNING_SECRET --repo MatrixDJ96/bazzite-mx --body "$(cat cosign.key)"` — secret name and the `--new-bundle-format=false` cosign flag must match `reusable-build.yml` | Phase 14 (repo recreation) |
| 18 | A `/usr/lib/bootc/kargs.d/*.toml` shipped in the image never lands in `/proc/cmdline` on a running system (`kvm.ignore_msrs` stayed at the kernel default `N`) | `kargs.d` TOMLs are processed only by `bootc install` / `bootc switch` / `bootc upgrade`; a deployment managed via rpm-ostree (rebase + layered packages, upgraded through rpm-ostree) never reads them | For loadable-module parameters, ship a modprobe.d file instead (`system_files/usr/lib/modprobe.d/bazzite-mx-kvm.conf`, `options kvm ignore_msrs=1 report_ignored_msrs=0`) — kmod applies it at every module load on every deployment kind. Reserve kargs.d for true kernel-cmdline needs (builtin modules, initramfs-time params). Verified live 2026-07-02 on a Katana 17 running `bazzite-mx-nvidia-open:stable` via rpm-ostree | Live-system audit (KVM kargs) |
| 19 | After `gh repo delete`, the 3 GHCR container packages remain visible on the user profile and the next CI build creates duplicates | GHCR packages are entities separate from the repo; the repo's delete does NOT cascade to them. They also retain their visibility (public/private) across delete+recreate of a same-name repo | Always purge packages explicitly: `gh api -X DELETE /user/packages/container/{bazzite-mx,bazzite-mx-nvidia,bazzite-mx-nvidia-open}` (3 calls, parallelizable) | Phase 14 (repo recreation) |
| 20 | `gh repo delete` → `HTTP 403: Must have admin rights to Repository` even though we are the owner | The default `gh` token from `gh auth login` ships scope `repo` only. `delete_repo` is a separate scope that must be granted explicitly | One-time `gh auth refresh -h github.com -s delete_repo` (interactive browser OAuth), then confirm scope in `gh auth status` before retry. Fallback: delete via UI (Settings → Danger Zone) — always works | Phase 14 (cleanup) |
| 21 | `make_latest: true` (unquoted YAML bool) passed to `softprops/action-gh-release@v3` is parsed inconsistently across action/runner versions and can land as `latest=false` | The action input is typed as string with allowed values `"true" \| "false" \| "legacy"`; YAML bool serialization in some paths yields the literal `true` token, in others string `"true"` — only the latter matches | Always pass a string. Our `generate-release.yml` uses the expression form `make_latest: ${{ steps.inputs.outputs.stream_name == 'stable' }}` — string-valued, equivalent to the quoted literal. Bazzite/Aurora quote the literal (`make_latest: "true"`) | Phase 14 (generate-release.yml) |
| 22 | Release changelog's "previous version" always resolved to the **oldest** release (e.g. every testing release pointed back to `testing-44.20260514`), skipping all intermediate ones | `generate-release.yml` sorted releases with `sort_by(.createdAt) \| last`, but a GitHub Release's `createdAt` is the **target commit's date, not the publish time**. Every upstream rebuild tags the same unchanged `main` HEAD (`target_commitish: github.sha`), so all `createdAt` values are identical → the sort is a full tie and jq's stable sort returns the last input element (the oldest). Same trap makes the `git log PREV..HEAD` Commits table always empty (PREV tag == HEAD commit). | Sort by `publishedAt` (monotonic, distinct per release) everywhere a "latest/previous" is picked over our own releases. The real inter-release delta is the **upstream** bump, so `changelog.sh` renders an `### Upstream` compare link (`prev_upstream...upstream`) and shows the downstream Commits table only when non-empty. The resolve jobs 404-gate `upstream_tag` against real `ublue-os/bazzite` releases, and `changelog.sh` uses it verbatim. | Release changelog audit (commit `e8ada14`) |
| 23 | Dropping the `qemu` metapackage from `20-virtualization.sh` does not slim the image: every `qemu-system-<arch>` emulator is still installed | `podman-bootc` (installed by `10-container-runtime.sh`) declares a hard rpm `Requires: qemu`, so dnf pulls the full metapackage — and with it all per-arch system emulators — as a dependency, regardless of the explicit virt package list | Keep the explicit list (it requests only what the virt stack needs); the meta rides in via podman-bootc and disappears on its own if that dependency is ever dropped upstream. A real size diet would require dropping podman-bootc | Repo update pre-flight verification (2026-07-01) |
| 24 | Release body cited the wrong upstream tag (`testing-44.20260705` instead of `testing-44.20260705.1`) and mislabelled the build "Downstream rebuild … no upstream changes" | A syntactic `.N`-strip in `changelog.sh` assumed any three-group tag is a downstream rebuild — but upstream Bazzite publishes its **own** same-day `.N` rebuild tags (`testing-44.20260705.1`, `testing-44.20260629.1`, …), so a trailing `.N` is syntactically ambiguous: no regex can tell whose it is | Downstream release tags carry the **build date** (`[testing-]MAJOR.<build date>[.N]`), so the `.N` is always ours and `IS_REBUILD` is decided on our nomenclature alone. `upstream_tag` flows verbatim (404-gated by the resolve jobs against real upstream releases); the previous release's upstream is read from its title (`… (Bazzite <tag>)`) | Release audit after pipeline reset (2026-07-06) |

## Patterns to be wary of

These are not yet bugs we've hit, but they're shape of bugs we expect:

- **systemd preset behaviour**: when COPR packages enable units via
  `systemd-preset` on install, our explicit `systemctl enable` after
  the install is then redundant — but it's defense-in-depth and
  harmless. Keep redundancy intentional.

- **Network-fetched RPMs**: every CI build re-downloads any RPM fetched
  via URL. Trust model rests on the source's HTTPS not being hijacked.
  Watch for sudden image-size jumps or smoke-test regressions that
  could indicate a changed upstream artifact.
