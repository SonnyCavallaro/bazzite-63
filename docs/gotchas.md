# Gotchas — canonical list

A discovered pitfall is documented here so the next session does not re-discover it the hard
way. Add new rows as they emerge, with the date and the commit / context where the lesson was
learned.

| # | Symptom | Root cause | Fix | First seen |
|---|---|---|---|---|
| 1 | Phase 2 v1 build failed: KCM branding test — "expected Variant=…, got ''" | `/usr/share/kcm-about-distro/kcm-about-distrorc` doesn't exist on Bazzite (Aurora-only path). Initial workaround used a `[ -f ]` guard that made the branding step silently no-op. | Bazzite ships the file at `/etc/xdg/kcm-about-distrorc` (the Bazzite-DX-style location). `build_files/mx/00-image-info.sh` seds `Variant` and `Website` there + updates `image-info.json` (image-name + image-ref + image-vendor) and `/usr/lib/os-release` VARIANT_ID. Smoke test asserts all six values (image-name, image-vendor, image-ref, VARIANT_ID, Variant, Website) to prevent silent regression. | Phase 2 (branding) |
| 2 | `dnf5 config-manager setopt <id>.enabled=0` returns 0 but the .repo file is unchanged | dnf5 5.x: setopt is a silent no-op on .repo files added via `addrepo --from-repofile=URL` or `--repofrompath` | `sed -i 's/^enabled=1/enabled=0/g' /etc/yum.repos.d/<file>.repo` — same idiomatic pattern used everywhere we touch a runtime-added .repo | Phase 3 (container runtime) |
| 3 | `systemctl enable swtpm-workaround.service` → "Unit … does not exist" | The COPR `ublue-os-libvirt-workarounds` v1.1+ consolidated the historical separate `swtpm-workaround.service` into the single `ublue-os-libvirt-workarounds.service` (which is a SELinux relabel oneshot) | Don't enable the missing unit. Modern swtpm + libvirt work without a separate workaround service. The Aurora upstream still references the old name in their build script — likely they have stale code we should not blindly copy | Phase 4 (virt) |
| 4 | `bootc container lint` warns `nonempty-boot: Found non-empty /boot: extlinux` (and `nonempty-run-tmp: Found content in runtime-only directories (/run, /tmp)`) | `qemu-system-x86-core` drops an `extlinux` binary in `/boot` for VM bootloader templates; the build stage leaves transient content in /run and /tmp | Not tolerated any more: the lint runs with `--fatal-warnings` (Containerfile), so either warning FAILS the build, and `clean-stage.sh` sweeps both sources before it runs (`find /boot -mindepth 1 -delete` plus the /run and /tmp sweep). Seeing either warning again means the clean-stage sweep regressed, not that the residue is back to being harmless | Phase 4 (virt) + audit baseline 2026-07-06 |
| 5 | `ujust setup-virtualization virt-on` does nothing on bazzite-63 | Bazzite's upstream recipe is gated on `if ! rpm -q virt-manager \| grep -P "^virt-manager-"` — meaning **only** runs the kargs/swtpm/hooks setup if virt-manager is NOT RPM-installed. On bazzite-63, `20-virtualization.sh` installs virt-manager as RPM at build time, so the gate is permanently FALSE and the entire setup branch is skipped — silent no-op | (a) build-time enable libvirtd.service in `20-virtualization.sh` so the basic stack works on first boot without any recipe; (b) ship the KVM options via `system_files/usr/lib/modprobe.d/bazzite-63-kvm.conf` (upstream moved them from bootc kargs.d — rpm-ostree never reads kargs.d — to modprobe.d); (c) ship our `setup-virtualization` override in `system_files/usr/share/ublue-os/just/96-bazzite-mx-overrides.just`, with the gate dropped and the flatpak-install line removed (we have RPM), while `build_files/mx/55-justfile-reconcile.sh` removes the same-named upstream recipe from `84-bazzite-virt.just`. kvmfr/usbhp blocks kept verbatim from bazzite-dx; the VFIO ones carry the revert-symmetry fixes of gotchas #35-#37 | Phase 4 (virt L2 hardening) |
| 6 | `dnf5 install iotop` succeeds, but `rpm -q iotop` returns "not installed" | F44 replaced the Python `iotop` with the C rewrite `iotop-c-1.31`. `iotop` is now an alias provider that resolves to `iotop-c`. The binary installed at `/usr/bin/iotop` keeps the historic name; the package name does not. | Smoke test asserts `rpm -q iotop-c`; the install line in `40-dev-cli-rpms.sh` requests `iotop-c` directly | Phase 6 (CLI) |
| 7 | `curl -I https://release.gitkraken.com/...` returns HTTP 404 | Server rejects HEAD method | `curl -sL --range 0-1023 <url>` (GET partial) — also lets you `file` the bytes to verify it's an RPM | Phase 5 (IDE — fetched while validating GitKraken provenance) |
| 8 | `docker run` on a fresh deployment fails with "permission denied while trying to connect to the Docker daemon socket" even though `docker-ce` is installed and `docker.socket` is enabled | `docker-ce` from Docker Inc creates the `docker` group via an rpm postinstall scriptlet (`groupadd --system docker`). On rpm-ostree atomic systems, package postinstall scriptlets are SUPPRESSED to keep the OCI layer reproducible — so the group is never created. The `10-bazzite-63-groups.sh` hook then logs `WARNING: group docker not in /usr/lib/group; skipping` (visible in journal of first boot) and silently no-ops the docker membership grant. Aurora and Bazzite-DX inherit this gap (verified: neither ships a sysusers.d for docker). | Ship `/usr/lib/sysusers.d/bazzite-63-docker.conf` with a single line `g docker -` (creates the group via systemd-sysusers at sysinit.target, before our hook runs). Bump `version-script bazzite-63-groups system N` to invalidate the cached state on existing installations so the hook re-runs and adds wheel users to the now-existing docker group. | Phase 9 (bazzite-extras) |
| 9 | `ujust install-<x>` recipes shipped in `96-bazzite-63.just` are not visible to ujust | Bazzite's master `/usr/share/ublue-os/justfile` uses explicit `import` directives (no glob). Files in `/usr/share/ublue-os/just/` not registered in the master are silently ignored. | Add a build-time append step (`build_files/mx/56-justfile-import-63.sh`, companion of upstream's `55-justfile-reconcile.sh`) that idempotently registers our justfile in the master via `grep -qxF \|\| echo >> master`. Bazzite-DX and AmyOS do the same but without idempotency check. | Phase 10 (justfile) |
| 10 | Running `ujust install-rider` (or any `install-*` that calls a private helper) prints `error: Recipe '_pkg_layered' failed on line N with exit code 1` even when the install proceeds correctly | `just` always emits that line whenever a sub-recipe exits non-zero, even when the caller wraps the call in `if`. Our `_pkg_layered` helper used `jq -e` exit codes as a boolean signal — idiomatic in bash, but here it polluted the install-* output with a misleading "error" line. | Switch the helper to communicate via stdout: `@if jq -e ...; then echo yes; else echo no; fi`, always exits 0. Callers compare the string with `[ "$(just _pkg_layered <pkg>)" = "yes" ]`. Same DRY benefit, zero spurious lines. | Phase 10 (justfile) |
| 11 | `libsetup.sh::version-script` writes the versioned state file BEFORE the hook body runs. Under `set -euo pipefail`, a single failed command (transient marketplace timeout, missing skel file, …) aborts the hook AFTER the state has been committed → next login skips the hook → silent permanent disable | `libsetup.sh` design choice: state-first ensures atomicity and idempotency on success. The cost is that "failed run that already wrote state" is indistinguishable from "successful run" on next boot. | (a) Add `\|\| true` after each best-effort command (e.g., `code --install-extension X \|\| true`) — failure becomes benign, hook completes, state correctly reflects "I tried." (b) Guard ALL source paths used in the hook (`[ -e /etc/skel/... ]`). | Phase 11 (vscode-extensions hook code review) |
| 12 | DNF resolves `dnf5 install sunshine` (lowercase) successfully but `rpm -q sunshine` returns "package sunshine is not installed" | rpm-q is case-sensitive on the actual rpm name (`Sunshine` capitalized in the lizardbyte/stable COPR spec); DNF normalizes case at install time, masking the divergence. Smoke tests that use `rpm -q sunshine` would silently regress on every build. | Always use the **exact rpm-name capitalization** in `rpm -q` assertions (the Sunshine rpm in the lizardbyte COPR is capitalized: `rpm -q Sunshine`). Aurora's `01-packages.sh` uses lowercase `sunshine` in the COPR install (works due to DNF case-insensitivity). The image installs Sunshine from the `pvermeer/sunshine` COPR, whose rpm name is lowercase (`rpm -q sunshine`). | Phase 12 (Sunshine reintegration) |
| 13 | Bazzite's `cockpit.service` `Requires=cockpit-container.service` but no such file exists in `/usr/lib/systemd/system/` | The unit is generated at boot from `/usr/share/containers/systemd/cockpit-container.container` (a podman quadlet) by `podman-systemd-generator` | Don't fight Bazzite's design. `ujust cockpit enable` works. Adding host-side `cockpit-*` RPMs is unnecessary because the containerized `quay.io/cockpit/ws:latest` bundles all standard modules | Discovery while scoping potential Cockpit phase (intentionally skipped) |
| 14 | `podman build … > log 2>&1; echo BUILD_EXIT=$?` always reports 0 even when the build failed | Final `echo` is the last command; its exit-zero replaces the build's exit code in the shell's overall exit status | Use `BUILD_EXIT=$?; echo "BUILD_EXIT=$BUILD_EXIT"; exit $BUILD_EXIT` (the explicit `exit` propagates the real status). Or use `&&` to chain failure-honouring | Pre-flight wrapper iteration |
| 15 | `paths-ignore` did not skip CI for a docs commit | Commit included `.claude/settings.json` and `.gitignore`, neither of which matches `**.md`, `LICENSE`, or `docs/**`. GitHub paths-ignore: workflow runs if **any** file fails to match | Either: (a) extend paths-ignore to cover `.claude/**` and `.gitignore` for future free docs commits, or (b) accept the redundant CI run and move on | Docs-only commit that triggered CI |
| 16 | AmyOS ships vendored `vscode.repo` with `enabled=1` (their philosophy), opposite to ours | Different distro philosophy — they trust the source and let it auto-update via rpm-ostree layering | We keep `enabled=0` and `validate-repos.sh` enforces it. Diverge consciously, document the decision (we do, in `vscode.repo`'s context comment) | Phase 5 (IDE) — comparing AmyOS approach |
| 17 | After `gh repo delete` + `gh repo create`, the first push triggers builds that fail at the `Sign container image` step | GitHub Secrets are repo-scoped and encrypted with the repo's public key; a recreated repo has a new internal ID + new pubkey, so old secrets are NOT migrated even if recreation is immediate | Reload **before** the first push: `gh secret set SIGNING_SECRET --repo SonnyCavallaro/bazzite-63 --body "$(cat cosign.key)"` — secret name and the `--new-bundle-format=false` cosign flag must match `reusable-build.yml` | Phase 14 (repo recreation) |
| 18 | After `gh repo delete`, the GHCR container package remains visible on the user profile and the next CI build creates a duplicate | GHCR packages are entities separate from the repo; the repo's delete does NOT cascade to them. They also retain their visibility (public/private) across delete+recreate of a same-name repo | Purge the package explicitly: `gh api -X DELETE /user/packages/container/bazzite-63` | Phase 14 (repo recreation) |
| 19 | `gh repo delete` → `HTTP 403: Must have admin rights to Repository` even though we are the owner | The default `gh` token from `gh auth login` ships scope `repo` only. `delete_repo` is a separate scope that must be granted explicitly | One-time `gh auth refresh -h github.com -s delete_repo` (interactive browser OAuth), then confirm scope in `gh auth status` before retry. Fallback: delete via UI (Settings → Danger Zone) — always works | Phase 14 (cleanup) |
| 20 | `make_latest: true` (unquoted YAML bool) passed to `softprops/action-gh-release@v3` is parsed inconsistently across action/runner versions and can land as `latest=false` | The action input is typed as string with allowed values `"true" \| "false" \| "legacy"`; YAML bool serialization in some paths yields the literal `true` token, in others string `"true"` — only the latter matches | Always quote: `make_latest: "true"`. Same form used by `bazzite/generate_release.yml` and `aurora/generate-release.yml` | Phase 14 (generate-release.yml) |
| 21 | Release changelog's "previous version" always resolved to the **oldest** release (e.g. every testing release pointed back to `testing-44.20260514`), skipping all intermediate ones | `generate-release.yml` sorted releases with `sort_by(.createdAt) \| last`, but a GitHub Release's `createdAt` is the **target commit's date, not the publish time**. Every upstream rebuild tags the same unchanged `main` HEAD (`target_commitish: github.sha`), so all `createdAt` values are identical → the sort is a full tie and jq's stable sort returns the last input element (the oldest). Same trap makes the `git log PREV..HEAD` Commits table always empty (PREV tag == HEAD commit). | Sort by `publishedAt` (monotonic, distinct per release) everywhere a "latest/previous" is picked over our own releases. The real inter-release delta is the **upstream** bump, so `changelog.sh` renders an `### Upstream` compare link (`prev_upstream...upstream`) and shows the downstream Commits table only when non-empty. The resolve jobs 404-gate `upstream_tag` against real `ublue-os/bazzite` releases, and `changelog.sh` uses it verbatim. | Release changelog audit (commit `992dc09`) |
| 22 | Apps appended to `/usr/share/ublue-os/bazzite/flatpak/install` never get installed on a system that installed the stock Bazzite ISO and then `bootc switch`ed here | The list is consumed **only by Bazzite's ISO installer** (flatpaks come prebaked in the ISO). At runtime `bazzite-flatpak-manager` maintains remotes/blocklist/overrides but installs nothing; the only install recipe (`_install-system-flatpaks`) curls **Bazzite's own list from GitHub**, not the local file. Removals (blocklist filter) DO work at runtime — which is why bazzite-mx never hit this: it only removed | Ship `/usr/libexec/bazzite63-flatpak-manager` (per-ref `--or-update` install of the whole local list, flathub remote-add included) and run it **on demand** via `ujust install-default-flatpaks` / `ujust bazzite-63-setup` — no systemd unit: silent boot-time installs were deliberately dropped in favour of explicit user-driven setup | VM validation of the fork (first `bootc switch` test) |
| 23 | A file shipped in `system_files/etc/` silently replaced Bazzite's own copy (e.g. a static `/etc/xdg/mimeapps.list` wiped upstream's Bazaar `.flatpakref` handler) | `build.sh` rsyncs `system_files/` over `/` with overwrite semantics: any path that also exists in the base image is clobbered wholesale, including future upstream additions | Never ship a `system_files` copy of a file the base image owns — MERGE at build time from a numbered `mx/` script (idempotent grep-then-append, like the Flatpak list and the mimeapps entries in `68-flatpak-apps.sh`) and keep a smoke-test canary asserting a known upstream entry survived | Final pre-release audit |
| 24 | `ujust install-default-flatpaks` shows ONE transaction (the list's first entry, e.g. "Updating 2/2" for an app the ISO already ships) then "Default Flatpak set installed/updated" — but nothing else installs: every other app KO in `b63-status`, empty app menu even after reboot | `flatpak install` reads stdin even with `--assumeyes`. Inside the manager's `while read` loop (stdin = the ref list via process substitution) the first `flatpak` call swallows all remaining lines, the next `read` hits EOF and the loop exits cleanly → success message, one ref processed | Redirect the child's stdin (`flatpak install … < /dev/null`) and echo each ref as it is processed. Smoke test runs the manager against a stdin-eating `flatpak` stub and asserts one install invocation per list entry. Same trap applies to ANY stdin-reading command (ssh, ffmpeg, …) inside a `while read` loop | First from-zero VM install test |
| 25 | Konsole opens `pwsh` (the default profile) but inside it `mise`, `node`, `sqlcmd` — and on some sessions even `brew` — are not found, while the bash fallback sees everything; on accounts that predate the image `mise install` is also a silent no-op (zero runtimes, success exit) and `b63-status` still said OK when it only probed the mise binary | Two per-user gaps compound: (a) pwsh reads its own `$PROFILE` (`~/.config/powershell/profile.ps1`), never `/etc/profile.d` — so `99-mise.sh` and brew's profile.d wiring are bash-only, and the Konsole profile launches pwsh via a non-login `bash -c` that sources no profile at all; (b) `~/.config/mise/config.toml` ships in `/etc/skel`, which only reaches accounts created after the image | Ship a skel `profile.ps1` (brew on PATH + `mise activate pwsh \| Out-String \| Invoke-Expression`), seed it and the mise config for pre-existing accounts via the `23-bazzite-63-dev-config.sh` first-login hook AND from `setup-dev` itself; `b63-status` checks the config file and the pwsh profile, not just the binary | Pre-ThinkPad-switch review of `setup-dev` |
| 26 | Release body cited the wrong upstream tag (`testing-44.20260705` instead of `testing-44.20260705.1`) and mislabelled the build "Downstream rebuild … no upstream changes" | A syntactic `.N`-strip in `changelog.sh` assumed any three-group tag is a downstream rebuild — but upstream Bazzite publishes its **own** same-day `.N` rebuild tags (`testing-44.20260705.1`, …), so a trailing `.N` is syntactically ambiguous: no regex can tell whose it is | Downstream release tags carry the **build date** (`[testing-]MAJOR.<build date>[.N]`), so the `.N` is always ours and `IS_REBUILD` is decided on our nomenclature alone. `upstream_tag` flows verbatim (404-gated by the resolve jobs against real upstream releases); the previous release's upstream is read from its title (`… (Bazzite <tag>)`) | bazzite-mx audit 2026-07-06, absorbed with the new resolve/changelog machinery |
| 27 | `Build Stable` dies at the `Rechunk layers` step with `no space left on device` (paths under `/var/lib/containers` and `/sysroot/ostree`), while a sibling run on the same commit succeeds | Some `ubuntu-24.04` runners come up without the `/mnt` scratch disk. `container-storage-action` is `continue-on-error` and prints `::notice::/mnt mountpoint not detected. Skipping BTRFS loopback...` — podman storage then lands on the ~14 GB root disk, which dies late, mid-rechunk. A healthy run logs a ~65 GiB loopback at `/mnt/btrfs_loopbacks/var-lib-containers` | `gh run rerun <id> --failed` — the retry usually lands on a runner with `/mnt`. When triaging an ENOSPC build, grep the log for the `Skipping BTRFS loopback` notice first: the root cause is the missing mount, not image growth | Post-realign publish 2026-07-08 |
| 28 | Baking an RPM that installs under `/opt` (Chrome, Brave, 1Password, …) dies at unpack: `[RPM] failed to open dir opt of /opt/google/: cpio: mkdir failed` | On ostree/bootc images `/opt` is a symlink to `var/opt`, which does not exist at build time — RPM's cpio cannot create `/opt/<vendor>` through the dangling link. Even after a successful unpack the payload would land in `/var`, which `clean-stage.sh` wipes | `mkdir -p /var/opt` BEFORE the install, then relocate: `mv /var/opt/<vendor> /usr/lib/opt/<vendor>` + a tmpfiles.d entry `L+ /var/opt/<vendor> - - - - /usr/lib/opt/<vendor>` so the link is recreated at boot and the `/opt/...` paths baked into the app keep resolving. Pattern: AmyOS `build_files/fix-opt.sh` (61-chrome-rpm.sh applies it; smoke test asserts payload + tmpfiles line) | First baked-Chrome build (2026-07-09) |
| 29 | `systemd-sysusers` prints `Creating group 'docker' ...` at every run yet `getent group docker` stays empty; on the same machine a login manager (e.g. `plasmalogin`) can die with `PAM: Authentication service cannot retrieve authentication info` after an ill-advised cleanup | Two ostree-specific traps compound. (a) sysusers writes group+gshadow **transactionally**: ONE orphaned `/etc/gshadow` entry (group present in gshadow, absent from group) aborts the whole transaction at every boot — every pending creation (docker, libvirt, ccache, …) silently never lands. (b) On ostree the account DB is SPLIT: system users/groups live in `/usr/lib/passwd` + `/usr/lib/group` (nss-altfiles) with their shadow entries in `/etc/{shadow,gshadow}` — any cleanup comparing shadow files against `/etc/passwd`/`/etc/group` ALONE classifies those legit entries as orphans and deletes them, breaking PAM for every system account | Diagnose with `getent` (it sees both sides), never with the /etc files alone. Remove genuinely orphaned gshadow entries one by one (present in gshadow, absent from `getent group`); repair a damaged DB by re-adding locked entries for every NSS-known name: `getent passwd \| cut -d: -f1` → missing in /etc/shadow → append `user:!*:20644::::::` (same pattern for gshadow with `group:!::`) | VM post-upgrade triage (2026-07-10) |
| 30 | A font-presence guard that pipes `fc-list` into `grep -q <family>` fails exactly when the font IS installed (under `set -o pipefail` the script aborts, or the branch inverts) | `grep -q` exits at the first match and closes the pipe; a still-writing `fc-list` dies of SIGPIPE, so the pipeline status is non-zero under `pipefail` precisely on success | Capture instead of piping: `[ -n "$(fc-list 'Segoe UI Variable')" ]`. Same trap for any `<producer> \| grep -q` guard in a pipefail script (`find`, `flatpak list`, …) | Segoe UI Variable kit (ThinkPad), folded into the image 2026-07-13 |
| 31 | `make -C "$KSRC" M="$src" modules` prints `MODPOST Module.symvers`, exits **0**, and produces no `.ko` at all — the failure only surfaces later as `FAIL: /run/kmods/… missing` in the install script | The module's kbuild fragment is gated on a kernel config symbol: `obj-$(CONFIG_NTFS_FS) += ntfs.o`. Against a kernel that leaves it unset the line expands to `obj- += ntfs.o`, a variable kbuild simply never reads, so it compiles nothing and reports success. The module's own top-level `Makefile` hides this by doing `export CONFIG_NTFS_FS := m` in its external-build branch — the branch the direct `-C $KSRC M=` invocation deliberately bypasses (it exists to target the right kernel) | Force the symbol as a make variable: `KO_BUILD_ARGS=CONFIG_NTFS_FS=m` in the module's `source.env`, expanded into the `make` line by `build-kmods.sh`. The smoke test asserts `modinfo -F alias ntfs` = `fs-ntfs`, so an empty build cannot pass silently. `msi-ec` and `acpi_ec` are immune — their fragments use unconditional `obj-m +=` | bazzite-mx ntfsplus spike 2026-07-29, absorbed at the ntfsplus realign |
| 32 | With the ntfsplus module loaded and `ntfs` listed in `/proc/filesystems`, `mount -t ntfs /dev/X /mnt` still lands on **ntfs-3g**: `findmnt` reports `fuseblk`, and the kernel driver is never reached | `mount(8)` hands any type with a `/sbin/mount.<type>` helper to that helper before touching the kernel. `/usr/sbin/mount.ntfs` and `/usr/bin/mount.ntfs` are symlinks to `mount.ntfs-3g` → `/usr/bin/ntfs-3g` (FUSE). This applies to fstab entries and to `mount -t auto` too, since libblkid also reports the type as `ntfs` | `build_files/mx/72-ntfsplus.sh` deletes `/usr/sbin/mount.ntfs` and `/usr/bin/mount.ntfs` (usrmerge makes them one file: `/usr/sbin` → `bin`), so type `ntfs` reaches the kernel driver on **every** path — fstab, systemd `.mount` units, `mount -t auto`, udisks. `mount -i -t ntfs` stays the way to reach it on an image that still carries the helpers, and `-i` has no fstab equivalent (`mount(8)`: it is a command-line flag), which is why the removal happens in the image. The ntfs-3g PACKAGE stays installed — `libguestfs-appliance` requires it — and `mount.ntfs-3g` survives, so `mount -t ntfs-3g` is the explicit FUSE escape hatch | bazzite-mx ntfsplus spike 2026-07-29, absorbed at the ntfsplus realign |
| 33 | An NTFS volume mounted `umask=000` presents a mix of `777`, `755` and `555` instead of uniform `777`, and the two NTFS drivers disagree on a few of those objects | The mode bits come from the WSL metadata EAs `$LXMOD` / `$LXUID` / `$LXGID` (written by WSL and by both Linux drivers), not from `umask` / `fmask` / `dmask`: an object carrying `$LXMOD` reports exactly that mode and the mask never reaches it. Objects with no EA fall back to `0777 & ~mask`. On top of that, ntfs3 subtracts the write bits for the DOS read-only attribute. The two drivers differ in exactly two places: ntfs3 lets `$LXUID`/`$LXGID` override the mount's `uid=`/`gid=`, while ntfsplus lets the mount option win (the `NTFS_VOL_UID` / `NTFS_VOL_GID` guards in its `ea.c`); and ntfsplus reads no DOS read-only attribute at inode load, so a read-only-attributed object without `$LXMOD` reports `777` where ntfs3 reports `555` | Read the real source of a mode with `getfattr -m - -d <path>` before blaming the mask. Both differences run permissive-only — no object loses a bit, exec included — so `ntfs3` → `ntfs` in fstab preserves every access. Round-trip is lossless in both directions: a `755` file created through ntfsplus stores `$LXMOD=0100755` and ntfs3 reads it back identically. Measured 2026-07-30 on `ldesktop-matrix`, fresh mounts on both sides (a mount that is already up serves `$LXMOD` from the cached inode and shows the pre-EA mode) | bazzite-mx ntfs fstab switch 2026-07-30, absorbed at the ntfsplus realign |
| 34 | A Steam library or a Windows tree on NTFS is assumed to keep Windows' case-insensitive lookups under Linux | Both `ntfs3` and the NTFSPLUS `ntfs` driver mount **case-sensitive** by default — `ntfs3` prints no `nocase` in its effective options and NTFSPLUS prints `case_sensitive`. Windows' case-insensitivity is a Win32 API property, not an on-disk one | Nothing to fix for a driver swap: the two behave identically, so `ntfs3` → `ntfs` changes no lookup. Both accept `nocase` if case-insensitive lookup is ever wanted. Measured 2026-07-30 on `ldesktop-matrix`: on the live `ntfs3` Archivio `Projects` resolves and `projects` / `PROJECTS` do not; same result under NTFSPLUS, and `nocase` makes all three resolve on either driver | bazzite-mx ntfs fstab switch 2026-07-30, absorbed at the ntfsplus realign |
| 35 | `ujust setup-virtualization vfio-off` runs clean, yet `vfio_pci.ids` and `kvmfr.static_size_mb` survive the revert — a multi-GPU host keeps its GPU bound to `vfio-pci` across the reboot | The removal arguments for those two are built into shell variables as `--delete-if-present=\"$VAL\"`. Expanding a variable performs word splitting but **no quote removal**, so `rpm-ostree` gets a karg NAME containing literal `"` characters and matches nothing. `--delete-if-present` says nothing about a karg it does not find, so the failure emits no event. bazzite-dx's `84-bazzite-virt.just` carries the same construction | Build the argument list as a bash array and expand it as `"${KARG_ARGS[@]}"`, so each argument carries its quoting from the point it is constructed. Reproduced and fixed live 2026-08-05 on `ldesktop-matrix`: with the pre-fix recipe a full `vfio-on` → `vfio-off` cycle left both kargs behind; after the fix the state capture diffs identical to the pre-`vfio-on` one | bazzite-mx VFIO revert symmetry 2026-08-05, absorbed at the vfio realign |
| 36 | The same `vfio-off` never prints `Found VFIO ids in kargs` on a host that plainly carries `vfio_pci.ids=` | The extraction `rpm-ostree kargs \| sed -E 's/.+(vfio_pci.ids=.+\s)/\1/' \| awk '{ print $1 }' \| grep vfio_pci.ids` needs whitespace AFTER the value. When the karg sits last on the line `sed` does not match and emits the line untouched, `awk` then returns field 1 (an unrelated karg) and `grep` yields empty — indistinguishable from "this host has no ids". Karg order depends on what was appended last, so the same recipe works or fails per host | Split one karg per line (`tr ' ' '\n'`) and match anchored (`grep '^vfio_pci\.ids='`): order-independent, with no silent degradation path. Same treatment for `kvmfr.static_size_mb`. This defect and gotcha #35 share one symptom, which is why each hid the other | bazzite-mx VFIO revert symmetry 2026-08-05, absorbed at the vfio realign |
| 37 | `rpm-ostree initramfs --enable` discards the initramfs arguments the host already had | `--enable` sets the argument list to exactly what its own invocation carries; with no `--arg` that list is empty, so an existing `--hostonly --no-hostonly-cmdline` becomes `[]` | Read the state from `rpm-ostree status --json` (`.deployments[0]."regenerate-initramfs"` and `."initramfs-args"`), never from the prose of `rpm-ostree initramfs`, whose wording is localized. `vfio-on` dropped the `--enable` call entirely: no VFIO input exists for a local regeneration to consume, since the image bakes the modules in via `force_drivers` in `/usr/lib/dracut/dracut.conf.d/80-vfio.conf`. Observed 2026-08-05 on `ldesktop-matrix`, where the pre-fix `vfio-on` wiped both arguments | bazzite-mx VFIO revert symmetry 2026-08-05, absorbed at the vfio realign |
| 38 | Every hourly `Watch Upstream` run fails, and the only visible error is `Status check not okay!` in `Validate matrix` — a job that runs no build of its own | The `Build bazzite-mx-nvidia` job dies with `initializing source docker://ghcr.io/ublue-os/akmods:ogc-44-6.18.44-ogc1.1.fc44.x86_64: manifest unknown`. Upstream moves an individual flavour onto the LTS kernel branch whenever a driver lags the mainline bump: `bazzite-nvidia` rides `6.18.44-ogc1.1.fc44` (proprietary NVIDIA driver) while `bazzite` and `bazzite-nvidia-open` ride `7.2.0-ogc3.1.fc44`. Its akmods carrier is published under the `ogc-lts` flavour, and the Containerfile's `ARG KERNEL_FLAVOR=ogc` was never overridden by CI, so the tag composed for that one flavour named an image that does not exist | `.github/scripts/resolve-kernel-coords.sh` derives the flavour from the registry: it reads the base image's `ostree.linux` label and asks `skopeo list-tags` which akmods flavours actually carry that exact kernel NVRA, then emits `kernel_version`, `kernel_flavor` and `fedora_version` — the last one taken from the kernel's own `.fcNN` so the three parts of the akmods tag cannot drift apart. It aborts before the build with an explicit message when no carrier exists yet, instead of dying minutes in on an unreadable manifest. Per-image flavour tables stay out: upstream flips a flavour back and forth as drivers catch up. The split is upstream-canonical since akmods `afc983c` (ogc tracks the OGC `latest` kernel image, ogc-lts the `lts` one); `ogc-lts` publishes `common`/`extra`/`nvidia-lts` carriers but NO `nvidia-open` — benign here, the Containerfile pulls only the plain `akmods` (common) carrier | CI build repair 2026-08-19 |
| 39 | On ogc's first 7.2 kernel every out-of-tree module comes out stamped for a kernel directory that does not exist, and `10-tests-mx.sh` reports `ntfs vermagic '7.2.0-ogc1.1.fc44.x86_64 …' does not match kernel 7.2-ogc1.1.fc44.x86_64` | The kernel package is internally inconsistent. Measured inside `akmods:ogc-44-7.2-ogc1.1.fc44.x86_64`: `kernel-core` installs its modules in `/lib/modules/7.2-ogc1.1.fc44.x86_64` and `kernel-devel` unpacks to `/usr/src/kernels/7.2-ogc1.1.fc44.x86_64`, while that tree carries `include/config/kernel.release` and `include/generated/utsrelease.h` both reading `7.2.0-ogc1.1.fc44.x86_64` — the three-component form kbuild renders for a `SUBLEVEL=0` release. `UTS_RELEASE` is what modpost stamps into vermagic, so the module names the release the kernel was BUILT as while its modules were PACKAGED under the two-component name. That kernel loads no module at all, in-tree ones included: `modprobe` searches `/lib/modules/$(uname -r)`, and `uname -r` reports `UTS_RELEASE`. `7.1.5` and earlier never diverged, and ogc restored consistency from `7.2.0-ogc2.1` on | Nothing to fix downstream: no correct module can be built against such a tree, so the build failing IS the right outcome — wait for a consistent kernel. Passing `KERNELRELEASE=<modules dir>` on the `make` line looks like a fix and is inert: measured in a lab build against that exact tree, the flag left vermagic at `7.2.0-ogc1.1.fc44.x86_64` because vermagic comes from the compiled-in `utsrelease.h`, never from the make variable. What the round did add is coverage: the vermagic assertion in `10-tests-mx.sh` now guards `msi-ec` and `acpi_ec` as well as `ntfs`, which alone carried it — the other two would have shipped silently unloadable | CI build repair 2026-08-20 |
| 40 | On the deployed host every `ujust` invocation dies with `error: unknown start of token '' (U+0000)` at `justfile:40:1`, while the same image built and smoke-tested green | The master `/usr/share/ublue-os/justfile` shipped with its appended import tail replaced by NUL bytes: the file is modified in-build (55/56 append the downstream imports) so it lives copied-up in overlay, and the 6.17-azure kernel on runner image 20260816+ loses page writeback of those writes — in-build greps read the correct bytes from warm page cache while the committed layer carries the torn tail. Measured on release 44.20260826.1: 2267 bytes, the last 276 all NUL, text ends right after upstream's `95-bazzite-deck-session.just` import — all three downstream imports vaporized | `56-justfile-import-63.sh` (the LAST writer of the file) closes with `rewrite_fresh_inode` from `build_files/shared/writeback-helpers.sh` (cp + sync + rename — the remedy every script whose last write to a base-image file is in place applies, `conventions.md` § Writing base-image files; build.sh step 4 is the sqlite equivalent), and `check-image-integrity.sh` re-proves the COLD bytes inside the chunked image after rechunk: a NUL sweep over every build-mutated artefact, all three downstream imports present, a real `just --summary` parse. Reported to upstream, whose `55-justfile-reconcile.sh` carries the same guard. An already-deployed casualty needs a rebuilt image: `/usr` is immutable, so the fix ships only via a new release | ujust breakage on the ThinkPad after the 44.20260826.1 update (2026-08-26) |
| 41 | A build on kernel `7.2.0-ogc6.1` kernel-panics seconds after `Switching root` on any host with an NTFS volume in fstab — caps-lock blinking, nothing persisted to journal or pstore — while the build and its vermagic guard stay green. Measured by upstream bazzite-mx on its release `44.20260828` (three panics out of three across two hosts); release `44.20260826.2` of this image ships the same kernel with the same module pin, so the same first-mount panic waits behind any `-t ntfs` mount there, udisks auto-mounting a plugged NTFS disk included | The ntfsplus kmod pin (`ca149db6`, main's tip of 2026-07-20) predates upstream PR #78 "iomap: consolidate bio submission" (merged to `ntfs-next` only, 2026-07-27): under a 7.2 kernel the module NULL-dereferences a bio on its first mount. The fstab mount units fire early in boot and the panic lands before `systemd-journal-flush`, so the failed boots leave only a truncated journal and an empty pstore — the evidence lives on the console alone. Upstream isolated it by masking the two NTFS mount units via `systemd.mask=` kargs: the same deployment then boots in 63 s | Pin bumped to the `ntfs-next` merge `5685d5b7d` (2026-08-25, carries the bio fix; the 2026-08-27 series above it does not compile). Upstream proved it at runtime on a live `7.2.0-ogc6.1`: insmod of the rebuilt module, loop-mounted NTFS image, 8 MB write + 20 dirents + umount/remount checksum match, clean rmmod. Build-time guards cannot catch this class — vermagic and modinfo pass on a module whose breakage is a runtime API mismatch, and a real mount needs a booted target kernel | Realign to bazzite-mx 2026-08-31 (their production panic of 2026-08-28) |
| 42 | A local pre-flight exits 0 in seconds after a `build_files/` script changed, and the build scripts never ran: the log carries `Using cache` throughout and commits the SAME image id as the previous run | The final stage reaches the build scripts through `--mount=type=bind,from=ctx`, and buildah's layer-cache key for a `RUN` is the command string plus the parent layer — the bind-mounted CONTENT is not hashed into it. Editing `build.sh`, a `mx/*.sh` or `validate-repos.sh` therefore invalidates nothing, and the pre-flight reports a green build that proves the previous code | Force the final stage to re-run by changing a build arg it consumes BEFORE the `RUN` steps: `--build-arg VERSION=<tag>-verifyN` invalidates the `LABEL …image.version` layer and everything after it, while the expensive `kmod-builder` stage (which never reads `VERSION`) stays cached. Read the log before trusting the exit code: the script's own output (`Validating all repository files…`, `rewritten onto a fresh inode`, `MX smoke tests OK.`) is the proof it ran, and a fresh image id is the proof it committed something new. `--no-cache` also works and rebuilds the kmods too (~25 min) | Realign to bazzite-mx 2026-08-31 (their battery audit of 2026-08-28) |
| 43 | On the first `ubuntu-26.04` run every build job dies at `Sign container image` with `env var $COSIGN_PRIVATE_KEY not found`, while the step's own env block plainly carries the secret — and the same `sudo -E` chain had signed 14 releases on `ubuntu-24.04` | `ubuntu-26.04` replaces classic sudo with **sudo-rs 0.2.13**, whose `-E` does not propagate the caller's custom env vars at all (measured 2026-09-01 in an `ubuntu:26.04` container: single-line and multiline vars both dropped; `--preserve-env=VAR` lists do pass). Everything env-dependent under sudo silently degrades: `sudo -E podman login` ignores `REGISTRY_AUTH_FILE` and writes root's default auth path — which is why `sudo skopeo copy` still pushed (same default path) while non-sudo readers of the declared file would find nothing — and `sudo -E cosign` sees neither the key nor the auth file | Stop passing env through sudo. Login runs rootless and writes `$REGISTRY_AUTH_FILE` as the runner user; root consumers (`skopeo copy`) take `--authfile "$REGISTRY_AUTH_FILE"` expanded by the runner shell; `cosign sign` drops sudo entirely (it only talks to the registry — same pattern as `sign-image.yml`) so its env block reaches it unmediated. `sudo -E buildah build` lost the dead `-E` too (proven no-op: the build was green with `-E` inert) | First main run post-rewrite, 2026-09-01, run 33531119669 |
| 44 | The run AFTER the first cache save dies in ~3 min at `Hand restored cache to root`, `du: cannot read directory … Permission denied` — while the cold-start path (and therefore every pre-flight and every sandbox run before a save existed) sails through | The step chowns the restored `/var/tmp/buildah-cache-*` to `root:root` (0700 dirs) and THEN sizes it with a plain `du` as the runner user; under `set -euo pipefail` du's read errors are fatal. The cold branch exits before either line, so the defect was unreachable until the first successful `Save DNF metadata cache` — which the signing-dead run 33531119669 still performed, arming the warm path for its successor | `sudo du -sh` after the chown (the save-side twin at `Prepare cache for saving` is already correct: it chowns TO the runner before sizing). Ordering lesson: any step that flips ownership must do every subsequent read from the owning side | First warm-cache run, 2026-09-01, run 33535416639 |
| 45 | Both `Verify published signatures` jobs would 401 on every `skopeo inspect`/`cosign verify` of our own images the first time the GHCR packages exist as private — the negative control passes and the positive checks all die | `verify-published-signatures.sh` talks to ghcr.io with no credentials at all: the job asks for `packages: read` permission but nothing consumes the token — no login step, no env. It only ever worked because the packages were public; the 2026-09-01 purge deleted them whole, and recreated packages are born private | Same `Login to GHCR` step as the promote jobs (skopeo login into `$REGISTRY_AUTH_FILE`; both skopeo and cosign honour that env var), so verification never depends on package visibility. Caught by code inspection before it burned a run — the two prior first-contact failures (#43, #44) prompted the sweep of every never-exercised post-gate path | Post-gate audit after #43/#44, 2026-09-01 |
| 46 | An hourly `Watch Upstream` run dies with `cannot inspect bazzite-mx:stable - refusing to guess staleness` (skopeo: `manifest unknown`) while the repo and upstream are both quiet | The cron fired inside the window when `:stable` did not exist: the 2026-09-01 package purge deleted the GHCR packages whole, and the rebuild's promote job — the step that recreates the release tags — had not landed yet (the build matrix was still running). The loud abort is `watch-upstream.yml`'s own guard working as designed: with no published image to read the base label from, guessing staleness could spuriously trigger or suppress a release | Nothing to fix. Once promote lands, a rerun of the failed run (or the next hourly fire) is green: attempt 1 failed 17:44Z mid-build of run 33537087391, attempt 2 passed 18:56Z with no build triggered (base label already at upstream latest). The shape recurs after any future package purge or before the first-ever promote; read it as "promote not landed yet", not as a broken workflow | Repo-audit round 2026-09-01, run 33539517522 |
| 47 | The first sandbox run after the resolver refactor kills all 6 build jobs in ~5 s at `Resolve upstream tag`: `./.github/scripts/resolve-upstream-tag.sh: No such file or directory` (exit 127), while the script is committed, executable and byte-identical to the local copy that passed every test | The step was inserted where the inline resolver used to live — before `Checkout repository`. An inline `run:` block needs no tree, so the old position was fine; a script under `.github/scripts/` exists only after the checkout. The local pre-flight builds the image, never the workflow, so nothing short of a CI run could see it | Every step that calls a repo script runs after the job's checkout (the `Resolve upstream tag` step now follows it). A one-off static check — for each job, no `.github/scripts/` reference in a `run:` before an `actions/checkout` step — failed on the broken file and passes on the fix; any refactor that turns an inline block into a script re-asks the question | Rehistory push 2026-09-01, run 33569731380 |
| 48 | Every `Watch Upstream` run goes red at `35-git-tools.sh` with `sha256sum: WARNING: 1 computed checksum did NOT match` / `/tmp/gitkraken-amd64.rpm: FAILED` while the repo is untouched; the image stops following the Bazzite base for as long as it lasts | GitKraken's download URL is a "latest" redirect and the script pinned version + sha256 of one release: the vendor's next release changes the bytes behind the same URL, so the pin fails on every build until someone bumps it by hand — a time bomb armed at every GitKraken release, in a script that must run unattended from the 6-hourly watcher | No version/sha256 pin on a moving URL. The script downloads the current release and verifies the RPM's own header and payload digests (`rpm -K --nosignature`), logs the version it shipped, then installs — the form upstream also moved to. A vendor artefact only gets a checksum pin when its URL is versioned and stable | Watch Upstream runs 33709534335 → 33959353436, 2026-09-03 → 05 (ten red runs) |

## Patterns to be wary of

These are not yet bugs we've hit, but they're shape of bugs we expect:

- **`thirdparty_repo_install()` reuse**: the function in `copr-helpers.sh`
  has been hardened (`sed` instead of `setopt`) but is not yet used.
  When a future Phase introduces a third-party `.repo` install via the
  helper, it'll be the first real-world test. Watch for the usual
  culprits: file naming mismatch, GPG check flag missing,
  `dnf5 install --nogpgcheck --repofrompath` semantics.

- **systemd preset behaviour**: when COPR packages enable units via
  `systemd-preset` on install, our explicit `systemctl enable` after
  the install is then redundant — but it's defense-in-depth and
  harmless. Keep redundancy intentional.

- **An upstream kernel that builds a module we ship**: already reality for `msi-ec` — the ogc
  7.2 kernel carries an in-tree `kernel/drivers/platform/x86/msi-ec.ko` beside ours under
  `updates/` (measured by upstream bazzite-mx 2026-08-28: with the `updates/` copy removed,
  `modinfo` resolves to the in-tree one). `updates/` wins by depmod priority, which is the
  intended outcome (our copy tracks the author's tree, the in-tree one lags), and both the
  smoke test and cold-check section 7 assert `*/updates/*` resolution, so a silent reversal
  fails the build. `ntfs` may follow the day ogc turns `CONFIG_NTFS_FS` on; the signal to
  watch is the module's own changelog, not the build.

- **Network-fetched RPMs**: every CI build re-downloads any RPM fetched
  via URL. Trust model rests on the source's HTTPS not being hijacked.
  Watch for sudden image-size jumps or smoke-test regressions that
  could indicate a changed upstream artifact.
